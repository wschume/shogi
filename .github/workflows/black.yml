name: black
on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - 13-add-black-workflow

jobs:
  black:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: psf/black@stable
        with:
          options: "--check --verbose ."
